import csv
import html
import json
import os
import re

# We will define translation rules and dictionary entries
# and test them on all 4 CSV datasets to ensure 100% coverage.

print("Building translation rules and dictionary...")
